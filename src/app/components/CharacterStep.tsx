"use client"
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

/**
 * CharacterStep: Handles character generation step (TODO placeholder).
 */
export default function CharacterStep() {
  return (
    <div className="py-10 px-4 max-w-2xl mx-auto">
      <Card>
        <CardHeader>
          <CardTitle>Step 2: Character Generation (TODO)</CardTitle>
        </CardHeader>
        <CardContent>
          <div>Character generation page placeholder. (Coming soon)</div>
        </CardContent>
      </Card>
    </div>
  )
} 